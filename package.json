{
  "name": "minifuse",
  "version": "0.1.0",
  "description": "Pipe a JSON config object on stdin, override pieces with CLI flags — minimist plus defaults < stdin < flags precedence",
  "main": "./lib/minifuse.cjs",
  "exports": {
    ".": {
      "import": "./lib/minifuse.mjs",
      "require": "./lib/minifuse.cjs"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "minimist": "^1.2.8"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
